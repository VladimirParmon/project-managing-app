export enum DialogFormControls {
  title = 'title',
}

export enum DialogDataOperations {
  create = 'create',
}

export enum DialogDataLabels {
  board = 'board',
  column = 'column',
  task = 'task',
}

export enum DialogDataTitles {
  board = 'Create new board',
  column = 'Create new column',
}
